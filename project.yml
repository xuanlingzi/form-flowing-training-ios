name: FormFlowing
options:
  bundleIdPrefix: com.formflowing
  deploymentTarget:
    iOS: "26.4"
  xcodeVersion: "26.4"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: 1

targets:
  FormFlowing:
    type: application
    platform: iOS
    sources:
      - path: FormFlowing/Sources
      - path: FormFlowing/Assets.xcassets
    settings:
      base:
        INFOPLIST_GENERATION: true
        PRODUCT_NAME: FormFlowing
        PRODUCT_BUNDLE_IDENTIFIER: com.formflowing.app
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait"
        INFOPLIST_KEY_NSAppTransportSecurity_AllowArbitraryLoads: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
